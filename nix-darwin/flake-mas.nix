{
  programs.mas = {
    enable = true;
    cleanup = true;
    update = false;

    packages = {
      # List every Mac App Store application that should be preserved.
      Infuse = 1136220934;
    };
  };
}
