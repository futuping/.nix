{
  programs.mas = {
    enable = true;
    cleanup = true;
    update = false;

    packages = {
      Infuse = 1136220934;
      # List every Mac App Store application that should be preserved.
    };
  };
}
